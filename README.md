# mailing-with-python

This is a script in python used or sending emails to any gmail id using the smtp library in python.
For this the user needs an active gmail account to be used for sending mails.
Then he needs to get connected gmail servers using smtp library as the code will illustrate.
